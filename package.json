{
  "name": "discoverify",
  "workspaces": [
    "backend",
    "frontend",
    "landing"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build:backend": "turbo build --filter=backend"
  },
  "dependencies": {
    "turbo": "^1.8.4"
  }
}
