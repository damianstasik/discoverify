{
  "name": "discoverify",
  "workspaces": ["backend", "frontend", "scripts"],
  "scripts": {
    "dev": "turbo run dev",
    "build:backend": "turbo build --filter=backend"
  },
  "devDependencies": {
    "@biomejs/biome": "1.3.1",
    "turbo": "^1.10.16"
  }
}
