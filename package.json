{
  "name": "discoverify",
  "workspaces": [
    "backend",
    "frontend",
    "landing",
    "scripts"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build:backend": "turbo build --filter=backend"
  },
  "dependencies": {
    "turbo": "^1.9.0"
  }
}
